# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

before_install:
  - cd code
language: R
sudo: false
cache: packages
addons:
  apt:
    packages:
    - libcurl4-openssl-dev
